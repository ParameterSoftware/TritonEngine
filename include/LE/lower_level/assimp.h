//Unless I use a Reflection Library (very discouraged in C(++)), assimp can't be assigned to the Leak namespace
//! TODO: import all of assimp into Leak::AImp
#include <iostream>
namespace Assimp = Leak::AImp;